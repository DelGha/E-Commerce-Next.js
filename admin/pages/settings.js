import Layout from "@/components/Layout";

export default function Settings(){
    return(
        <Layout>
            <h1>
                Settings Page: Work in Progress!
            </h1>
        </Layout>
    );
}